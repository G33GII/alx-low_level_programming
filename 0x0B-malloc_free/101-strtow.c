#include "main.h"

/**
 * strtow - 
 * @str: 
 * 
 * Return: 
 * 
*/

char **strtow(char *str)
{                                                                                                   /* "      ALX School         #cisfun      " */
    char **rz;                                                                                       /* int** arr = (int**)malloc(r * sizeof(int*)); */   
    int x, z, xl;                                                                                      /* for (i = 0; i < r; i++) */ 
                                                                                                        /* arr[i] = (int*)malloc(c * sizeof(int)); */
    if (!str || str == "")
        return (NULL);


    







}