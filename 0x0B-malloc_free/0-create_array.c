#include "main.h"

/**
*
*
*/

char *create_array(unsigned int size, char c)
{
    char *a;

    if (size == 0)
        return (0);
    
    a = malloc (sizeof(char) * size);

    if (a == NULL)
        return (0);


    





}